console.log("Hello World!")

// Repositório remoto -> Github
// Repositório local -> seu computador

// git init - para inicializar o repositório (1 vez)

// git add <nomedoarquivo ou pasta>
 // git add hello-world.js
 // git add . -> adicionar todos arquivos

// git commit -m "Adição de arquivos"

// git branch -M main -> para utilizar 1 vez a depender da necessidade

// git remote add origin https://github.com/edmaaralencar/teste.git (conectar repositório remoto com local) (só roda 1 vez)

// git push -u remote main